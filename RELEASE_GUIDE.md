# 🚀 自动化发布指南

基于之前的发布经验，我们创建了两个自动化发布脚本，让您可以傻瓜式地进行版本更新。

## 📋 脚本说明

### 1. `auto-release.js` - 完整发布工具
- **适用场景**: 重要版本发布，需要自定义版本号
- **功能**: 完整的发布流程，支持多种版本类型
- **特点**: 交互式选择版本类型，详细的发布说明

### 2. `quick-release.js` - 快速发布工具
- **适用场景**: 日常小版本更新
- **功能**: 自动计算下一个版本号，简化流程
- **特点**: 一键发布，适合频繁的小版本更新

## 🔧 使用前准备

### 1. 获取GitHub Token
1. 访问: https://github.com/settings/tokens
2. 点击 "Generate new token (classic)"
3. 勾选权限:
   - ✅ `repo` (完整的仓库访问权限)
4. 点击 "Generate token"
5. 复制生成的token

### 2. 确保依赖已安装
```bash
npm install axios
```

## 📝 使用方法

### 方法一：快速发布（推荐日常使用）

```bash
node quick-release.js
```

**流程**:
1. 自动计算下一个版本号（如 1.0.4 → 1.0.5）
2. 输入GitHub Token
3. 确认发布
4. 自动执行：更新版本号 → 构建 → 打包 → 生成yml → 发布到GitHub

### 方法二：完整发布（推荐重要版本）

```bash
node auto-release.js
```

**流程**:
1. 选择版本类型：
   - `patch`: 补丁版本 (1.0.4 → 1.0.5)
   - `minor`: 次要版本 (1.0.4 → 1.1.0)
   - `major`: 主要版本 (1.0.4 → 2.0.0)
   - `custom`: 自定义版本号
2. 输入GitHub Token
3. 自动执行完整发布流程

## 🎯 自动化流程

两个脚本都会自动执行以下步骤：

1. **📝 更新版本号**
   - 修改 `package.json` 中的版本号
   - 自动提交所有更改到Git
   - 创建Git标签
   - 推送到远程仓库

2. **🔨 构建应用**
   - 运行 `npm run build`
   - 编译React应用

3. **📦 打包应用**
   - 运行 `npm run package:mac`
   - 生成DMG和ZIP文件

4. **📄 生成更新文件**
   - 运行 `node generate-yml.js`
   - 生成 `latest-mac.yml` 文件

5. **🚀 发布到GitHub**
   - 创建GitHub Release
   - 上传所有文件
   - 生成发布说明

## 📊 发布示例

### 快速发布示例
```bash
$ node quick-release.js

⚡ 快速发布工具
================
当前版本: 1.0.4
📦 新版本号: 1.0.5
请输入GitHub Token: ghp_xxxxxxxxxxxxx
✅ Token验证成功，用户: imhaisu
确认发布 v1.0.5? (y/N): y

🚀 开始自动化发布流程...
📝 更新版本号...
📝 提交所有更改...
📝 创建标签 v1.0.5...
📤 推送到远程仓库...
✅ 版本号更新和Git操作完成
🔨 构建和打包应用...
✅ 构建完成
✅ 打包完成
📄 生成latest-mac.yml...
✅ latest-mac.yml生成完成
🚀 发布到GitHub...
✅ Release创建成功
📤 上传: Readix-1.0.5-arm64.dmg
✅ Readix-1.0.5-arm64.dmg 上传成功
📤 上传: Readix-1.0.5-arm64-mac.zip
✅ Readix-1.0.5-arm64-mac.zip 上传成功
📤 上传: latest-mac.yml
✅ latest-mac.yml 上传成功

🎉 发布完成！
📋 Release页面: https://github.com/imhaisu/Readix/releases/tag/v1.0.5
```

## ⚠️ 注意事项

### 1. 版本号规范
- 必须符合语义化版本规范 (x.y.z)
- 建议使用快速发布工具，自动计算版本号

### 2. 网络要求
- 确保网络连接稳定
- GitHub API可能有速率限制

### 3. 文件检查
- 脚本会自动检查必要文件是否存在
- 如果文件缺失，会显示警告信息

### 4. 错误处理
- 如果发布失败，会显示详细的错误信息
- 可以重新运行脚本重试

## 🔄 测试更新

发布完成后，您可以测试更新功能：

1. **开发模式测试**:
   ```bash
   npm run dev
   # 进入设置页面测试更新流程
   ```

2. **生产环境测试**:
   - 下载旧版本应用
   - 启动应用，进入设置页面
   - 点击"检查更新"
   - 验证更新流程

## 📈 版本管理建议

### 版本类型选择
- **patch (补丁)**: 修复bug，小改进
- **minor (次要)**: 新功能，向后兼容
- **major (主要)**: 重大更新，可能不兼容

### 发布频率
- **日常更新**: 使用 `quick-release.js`
- **重要版本**: 使用 `auto-release.js`
- **建议频率**: 每周或每两周发布一次

## 🎉 优势

1. **🚀 一键发布**: 无需手动执行多个命令
2. **🔒 安全可靠**: 自动验证Token和文件
3. **📝 自动生成**: 发布说明和更新文件
4. **🔄 完整流程**: 从版本更新到GitHub发布
5. **⚡ 快速高效**: 减少人为错误，提高效率

现在您可以轻松地进行版本发布了！🎊 